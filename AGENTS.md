# 开发约束

先读 docs/PRODUCT_CONTRACT.md 和 docs/IMPLEMENTATION_PLAN.md。
如果本地存在 docs/PROGRESS.md，继续读取当前进度；该文件不提交，缺失时不要假定后续里程碑已获授权。
只推进当前获授权里程碑，等待用户验收后进入下一阶段。中文沟通。
使用固定栈、uv 项目隔离环境；不改其他项目，不升级全局环境，不覆盖用户修改。
业务规则先写失败测试，再最小实现，再运行 pytest、Ruff 和实际 HTTP 验证。
页面/API → 应用服务 → 业务规则；基础设施作为适配器，避免提前抽象。
未知事实不得补造；任务和审批分离；密钥不入 Git/页面/日志。模板保持转义。
禁止未经单独授权的付费模型调用、客户资料上传、公开部署/仓库推送、大模型安装、破坏性 Git 操作。
每部分提供简短学习说明：业务场景、代码入口、知识点和面试表达边界。Cursor 用于用户学习阅读，不视作并行开发。
记录真实执行结果和限制，不伪造测试、客户或商业成绩。不要添加无用页面和接口。

提交代码时保留运行代码、测试、迁移、依赖锁文件、README 和项目约束。
学习笔记 docs/LEARNING*.md 与过程记录 docs/PROGRESS.md 只留本地，不提交；不得删除它们。
