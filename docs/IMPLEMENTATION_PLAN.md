# 实施计划

## M1（当前）
1. 契约、环境和锁文件。
2. 商品输入规则：必填、长度、空值、款号唯一；先红后绿。
3. SQLite 模型和 Alembic 初始迁移：商品、持久化任务、草稿版本。
4. 服务层与商品录入、列表、详情页；明确服务未配置。
5. pytest、Ruff、迁移往返和本地 HTTP 检查；学习记录和用户手动验收。

## M2（未授权开始）
确认提供商、模型 ID、预算、数据范围；真实生成、结构与事实风险检查、持久化执行和中断恢复、展示失败原因。独立真实冒烟验证。

## M3（未开始）
版本编辑、审批记录、退回、指定已通过版本导出；验证新版本不继承审批。

## M4（未开始）
补全冲突/注入/错误输出/超时/重复提交/重启/导出/审批隔离测试矩阵，整理可复现证据与演示。
